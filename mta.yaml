_schema-version: "3.2"
ID: vendorforecast
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: vendorforecast-destination-content
  type: com.sap.application.content
  requires:
  - name: vendorforecast-destination-service
    parameters:
      content-target: true
  - name: vendorforecast-repo-host
    parameters:
      service-key:
        name: vendorforecast-repo-host-key
  - name: vendorforecast-uaa
    parameters:
      service-key:
        name: vendorforecast-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: vendorforecast_html_repo_host
          ServiceInstanceName: vendorforecast-html5-service
          ServiceKeyName: vendorforecast-repo-host-key
          sap.cloud.service: vendorforecast
        - Authentication: OAuth2UserTokenExchange
          Name: vendorforecast_uaa
          ServiceInstanceName: vendorforecast-xsuaa-service
          ServiceKeyName: vendorforecast-uaa-key
          sap.cloud.service: vendorforecast
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: vendorforecast-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: vendorforecast-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - vendorforecast.zip
      name: vendorforecast
      target-path: resources/
- name: vendorforecast
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: vendorforecast-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: vendorforecast-destination-service
    service-plan: lite
- name: vendorforecast-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: vendorforecast-xsuaa-service
    service-plan: application
- name: vendorforecast-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: vendorforecast-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
